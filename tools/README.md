# Tools

여기는 실제로 일을 하는 파이썬 파일들이 저장되는 곳입니다 — API 호출, 데이터 변환, 리포트 생성, 이메일 발송 등.

규칙:

- 각 Tool은 하나의 일을 잘해야 합니다. 여러 Workflow에서 재사용할 수 있게 만듭니다.
- 새 Tool을 만들기 전에, 이미 비슷한 게 있는지 이 폴더를 먼저 확인합니다.
- 비밀 키(API 키 등)는 절대 Tool 코드에 직접 쓰지 않습니다. `.env`에서 불러옵니다.

아직 Tool이 없습니다. 필요한 업무가 생기면 여기에 추가됩니다.
